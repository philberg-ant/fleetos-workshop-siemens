# FleetTracker

Internal vehicle telemetry & maintenance scheduler. Flask + MySQL.

- `python app.py` then http://localhost:5000
- `/status` - fleet overview
- `/admin` - recalc + manual overrides
- DB config in `settings.py`

Contact HB for prod credentials.